package dev.infinia.store.contract.type;

/** Target operating system of an artifact. UNIVERSAL artifacts are platform independent. */
public enum Platform {
    WINDOWS,
    MACOS,
    LINUX,
    UNIVERSAL
}
